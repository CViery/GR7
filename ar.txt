uri = mongodb+srv://cristian:viery2312@gr7.jfleg90.mongodb.net/?retryWrites=true&w=majority&appName=gr7



from pymongo.mongo_client import MongoClient
from pymongo.server_api import ServerApi
uri = "mongodb+srv://cristian:viery2312@gr7.jfleg90.mongodb.net/?appName=gr7"
# Create a new client and connect to the server
client = MongoClient(uri, server_api=ServerApi('1'))
# Send a ping to confirm a successful connection
try:
    client.admin.command('ping')
    print("Pinged your deployment. You successfully connected to MongoDB!")
except Exception as e:
    print(e)





PLACA(input),MODELO VEICULO(input),DATA ORÇAMENTO(input data),DATA FATURAMENTO(input data),	DIAS(data orçamento - data faturamento),	NUM_OS(input)	CIA(select)	CONVERSÃO PNEUSTORE(se Cia for pneu store abrir campo para coletar se é pneustore-pneu ou pneustore serviço),	 PECAS(input), 	 SERVICOS(input), 	 VALOR(mostrar aqui o resultado da soma do input pecas com o input serviços),
	 REVITALIZAÇÃO(input), ADITIVO(input) 	QNTDE(se ter valor em Aditivo abrir campo de quantidade), FLUIDO/SANGRIA(input), PALHETA(input),LIMPEZA DOS FREIOS (input), 	 DETERGENTE DE PARABRISA(input) FILTRO(pedir valor de filtro serviço e filtro peça se houver filtro),PNEUS(input) BATERIA(input) 	MODELO(se houver valor em bateria abrir um select com os modelos),QUANTIDADE/OLEO(input), VALOR OLEO(se houver valor em quantidade oleo aparecer), TIPO/MARCA(se houver valor em quantidade oleo aparecer), Mecanico( Select), Filtro mecanico(select)
   	 VALOR Meta(aqui é para mostrar o valor dessa conta valor total - (bateria + pneu) se não houver valor em bateria e penu mostrar o valor total),VALOR EM DINHEIRO(input)
  FREIOS(input),SUSPENSÃO(input), INJEÇÃO/IGNIÇÃO(input), CAbeçote motor arrefecimento(input), OUTROS(input),OLEOS(input),TRASMISSÃO(input) 



  faturamento_mecanico=dados_mecanicos,
                                    faturamento_companhia=dados_companhias, 
                                    faturamento_servicos=dados_servicos,
                                    valor_faturamento_total=valor_faturamento_total,
                                    valor_faturamento_meta=valor_faturamento_meta,